/**
 * app/radar/index.tsx — Écran principal Radar
 * Thin wrapper → Dashboard (logique dans src/screens/).
 */

import Dashboard from '../../src/screens/Dashboard';

export default function RadarIndex() {
  return <Dashboard />;
}
